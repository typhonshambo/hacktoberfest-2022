var object = {} // time complexity O(1)

object.firstName = "Mohd"  // object key => insertion O(1) constant
object.lastName = "Sadab"
object.age = 23;
object.designation = "Full Stack Developer"  

// access time complexity  
console.log(object.firstName.object['firstName']) 

// deletion time complexity is also O(1)

// searching time complexity in Object is O(n);

// if any key have value 23;   

// time complexity of Object.keys is O(n);

// time complexity of Object.values is O(n);

// time complexity of Object.entries is o(n);

// time complexity of Object.hasOwnProperty('age') is O(1);